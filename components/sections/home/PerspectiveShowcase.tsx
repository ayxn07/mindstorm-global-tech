import PerspectiveGrid from "@/components/visuals/PerspectiveGrid";

export default function PerspectiveShowcase() {
  return (
    <section
      aria-label="Integrated expertise showcase"
      className="relative w-full"
    >
      <PerspectiveGrid
        heading="Integrated Hardware, Software, Network & Security Expertise"
        subheading="Scroll Through the Stack"
      />
    </section>
  );
}
